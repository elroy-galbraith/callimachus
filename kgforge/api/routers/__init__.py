"""Routers grouped by UI domain.

One module per Streamlit page (plus a generic ``jobs`` module for the
job-status + SSE endpoints). Phase A ships ``projects`` and ``settings``;
the rest land page-by-page in Phase B.
"""
